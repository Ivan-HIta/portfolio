# This project runs completely offline with synthetic data.
# Keep secrets out of source control. No API key is required for the default workflow.

APP_ENV=local
MODEL_DISPLAY_NAME=Synthetic Credit Risk Baseline
RANDOM_SEED=42

# Reserved for a future, approved LLM evaluation integration. Leave blank for this demo.
OPTIONAL_LLM_API_KEY=
