# Optional LLM configuration. Leave blank to use the built-in offline summarization fallback.
LLM_API_KEY=
LLM_MODEL=

# Optional local database override. The default is a SQLite database in the project folder.
DATABASE_URL=sqlite:///data/operations_copilot.db
