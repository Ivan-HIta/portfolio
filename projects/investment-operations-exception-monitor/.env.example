# The portfolio demo is local and uses fully synthetic data only.
# No API key is required for the default workflow.

APP_ENV=local
RANDOM_SEED=42
DEFAULT_SLA_HOURS=24

# Reserved for an optional future, approved integration. Leave blank for this demo.
OPTIONAL_SERVICE_TOKEN=
